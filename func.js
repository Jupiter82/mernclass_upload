const numbs = (1,2,3,4,5,6,7,8,9)

const getEven = (numbs) => {
    const evnArra = []

   numbs.map((value) => {
    if(value % 2 === 0)
    {
        evnArra.push(value)
    }
   })
   return value
}

console.log(getEven(numbs))


// recurssive functin sun 